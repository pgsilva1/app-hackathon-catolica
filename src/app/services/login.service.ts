import { Injectable, http } from '@angular/core';
import { API_URL } from './config';

@Injectable({
  providedIn: 'root'
})
export class LoginService {

  constructor() { 
    
  }

  doLogin(){
  }
}
